import React from 'react';
import { Link } from 'react-router-dom';

const GenerateRoutine = () => {
    return (
        <div>
            <h2>This is Generate Routine</h2>
            <Link to='/admin'>Go Back</Link>
        </div>
    );
};

export default GenerateRoutine;